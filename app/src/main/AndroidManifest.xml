<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.italac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.Historico_Analises"></activity>
        <activity android:name=".view.ResetLogin" />
        <activity android:name=".view.MenuFazenda" />
        <activity android:name=".view.MenuAnalise" />
        <activity android:name=".view.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ListFazenda" />
        <activity android:name=".view.CadUsuario" />
        <activity android:name=".view.CadFazenda" />
        <activity android:name=".view.CadAnalise" />
    </application>

</manifest>